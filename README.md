# Sensometrix

Static client and Supabase SQL migrations.

Open `index(2).html` through a web server. Configure `window.SENSOMETRIX_CONFIG` with the Supabase URL and publishable key before production use.
