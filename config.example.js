// Copy to config.js ONLY for a new deployment. Preserve your existing config.js on upgrades.
// This browser file may contain only a Supabase public/publishable or legacy anon key.
// Never put service_role keys, database passwords, or payment secrets here.
window.SENSOMETRIX_CONFIG = {
  supabaseUrl: 'https://YOUR_PROJECT.supabase.co',
  supabasePublishableKey: 'YOUR_SUPABASE_PUBLISHABLE_OR_ANON_KEY'
};
